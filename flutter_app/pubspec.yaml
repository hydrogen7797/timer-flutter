name: timer
description: 3-minute timer app (END / SLEEP / DECIDE variants).
publish_to: none
version: 1.1.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # Uncomment and add icon when ready:
  # assets:
  #   - assets/images/
